@Library('pipeline-library-demo')_

pipeline {
    agent any
    stages {
            stage("build") {
                steps {
                    echo "testing build"
					checkhuman 'Saurabh'
                    }
                }
            stage("test") {
                steps {
                    echo "testing application"
					checkhuman 'Riya'
                    }
                }
            stage("deploy") {
                steps {
                    echo "deploying application"
					checkhuman
                    }
                }
           }
}
