@Library('pipeline-library-demo')_

pipeline {
    agent any
    stages {
            stage("build") {
                steps {
                    echo "testing build"
					checkhuman 'human'
                    }
                }
            stage("test") {
                steps {
                    echo "testing application"
                    }
                }
            stage("deploy") {
                steps {
                    echo "deploying application"
                    }
                }
           }
}
